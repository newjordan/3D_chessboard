[build]
builder = "NIXPACKS"
buildCommand = "npm run generate && npm run build"

[deploy]
startCommand = "npm run start --workspace @chess-agents/worker"
restartPolicy = "always"

[nixpacks]
# Let Nixpacks layer Node automatically, or provide both if overriding
pkgs = ["nodejs", "python3"]
