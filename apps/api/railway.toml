[build]
builder = "NIXPACKS"
buildCommand = "npm run build && npx prisma migrate deploy --schema ../../prisma/schema.prisma"

[deploy]
startCommand = "npm run start --workspace @chess-agents/api"
healthcheckPath = "/api/leaderboard"
healthcheckTimeout = 100
restartPolicy = "on-failure"
